{% comment %}
  @liquidoc
  name: Ingredient Slide
  type: snippet
  description: Displays detailed information about an ingredient in a card format
  variables:
    - name: uses_metafields
      type: boolean
      description: Whether to render metafield content in the description
{% endcomment %}

{% stylesheet %}
.sb-ingredient-slide {
  background: var(--sb-ch-bg-color, #fff);
  border-radius: 8px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
  padding: 16px;
  position: relative;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  gap: 8px;
  height: calc(100% - 32px);
}

.sb-ingredient-slide__header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}

.sb-ingredient-slide__heading {
  font-size: 20px;
  font-weight: 600;
  color: var(--sb-fg-color, #111314);
  margin: 0;
  line-height: 1.4;
  flex: 1;
  padding-right: 16px;
}

.sb-ingredient-slide__close-btn {
  background: none;
  border: none;
  font-size: 24px;
  color: var(--sb-fg-color, #111314);
  cursor: pointer;
  padding: 0;
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: color 0.2s ease;
  flex-shrink: 0;
}

.sb-ingredient-slide__close-btn:hover {
  color: #333;
}

.sb-ingredient-slide__weight {
  color: #2563eb;
  font-size: 14px;
  font-weight: 500;
}

.sb-ingredient-slide__content {
  margin-bottom: 60px;
  max-height: 600px;
  overflow-y: auto;
  color: var(--sb-fg-color-lightened, #676767);
}

.sb-ingredient-slide__detailed-description {
  color: var(--sb-fg-color-lightened, #676767);
  font-size: 14px;
  line-height: 1.3;
}
.sb-ingredient-slide__detailed-description a {
  color: var(--sb-fg-color-lightened, #676767)!important;
}

.sb-ingredient-slide__detailed-description p {
  margin: 0 0 16px 0;
}

.sb-ingredient-slide__detailed-description p:last-child {
  margin-bottom: 0;
}

.sb-ingredient-slide__image {
  position: absolute;
  right: -30px;
  bottom: -20px;
  width: 110px;
  height: 110px;
  mix-blend-mode: multiply;
  z-index: 1;
}

.sb-ingredient-slide__image img {
  border-radius: 4px;
  max-width: 100%;
  height: auto;
}

.sb-ingredient-slide__divider {
  display: none;
}

/* Desktop Styles */
@media (min-width: 768px) {
  .sb-ingredient-slide {
    padding: 20px;
    position: relative;
    height: calc(100% - 40px);
  }
  
  .sb-ingredient-slide__heading {
    font-size: 24px;
  }
  
  .sb-ingredient-slide__weight {
    font-size: 16px;
  }
  
  .sb-ingredient-slide__detailed-description {
    font-size: 16px;
    margin-bottom: 40px;
  }
  
  .sb-ingredient-slide__image {
    position: absolute;
    bottom: -20px;
    right: -80px;
    width: 230px;
    height: 200px;
    margin: 0;
    text-align: right;
  }
  .sb-ingredient-slide__content {
    max-width: 700px;
    margin-right: auto;
  }
}

/* Large Desktop */
@media (min-width: 1440px) {
.sb-ingredient-slide__content {
    max-width: 900px;
    margin-bottom: 40px;
  }

}
{% endstylesheet %}

<div class="sb-ingredient-slide">
  <div class="sb-ingredient-slide__header">
    <h2 class="sb-ingredient-slide__heading">{{ settings.ingredient_title }}</h2>
    <button type="button" class="sb-ingredient-slide__close-btn" data-modal-close>
      <span aria-hidden="true">×</span>
      <span class="visually-hidden">Close</span>
    </button>
  </div>
  <div class="sb-ingredient-slide__weight">{{ settings.ingredient_weight }}</div>
  
  {% if settings.ingredient_detailed_description != blank %}
    <div class="sb-ingredient-slide__content">
      <div class="sb-ingredient-slide__detailed-description">
        {% if uses_metafields %}
          {{ settings.ingredient_detailed_description | metafield_tag }}
        {% else %}
          {{ settings.ingredient_detailed_description }}
        {% endif %}
      </div>
    </div>
  {% endif %}
  
  {% if settings.ingredient_image_small != blank or settings.ingredient_image_large != blank %}
    <div class="sb-ingredient-slide__image">
        {% if settings.ingredient_image_small != blank %}
          {{ settings.ingredient_image_small | image_url: width: 400 | image_tag }}
        {% elsif settings.ingredient_image_large != blank %}
          {{ settings.ingredient_image_large | image_url: width: 400 | image_tag }}
      {% endif %}
    </div>
  {% endif %}
  
  <div class="sb-ingredient-slide__divider"></div>
</div>