{%  stylesheet %}
  
  /* Content heading styles */
  .sb-latest-articles__container {
    --grid-desktop-vertical-spacing: 24px;
    --grid-desktop-horizontal-spacing: 24px;
    --grid-mobile-vertical-spacing: 24px;
    --grid-mobile-horizontal-spacing: 24px;
  }
  
  .sb-latest-articles__container .sb-latest-articles__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 24px;
  }
  
  .sb-latest-articles__title {
    font-size: 28px;
    font-weight: 600;
    margin: 0;
    line-height: 1.2;
  }
  
  .sb-latest-articles__view-all {
    display: flex;
    align-items: center;
    gap: 10px;
    font-family: var(--sb-font-family-body);
    font-size: 16px;
    font-weight: 500;
    line-height: 1;
    color: var(--sb-fg-color, #111314);
    text-decoration: none;
    border-bottom: 1px solid var(--sb-fg-color, #111314);
    padding-bottom: 4px;
    transition: opacity 0.2s ease;
  }
  
  .sb-latest-articles__view-all svg {
    width: 18px;
    height: 18px;
    transform: rotate(90deg);
  }
  
  /* Tab styles */
  .sb-latest-articles__container .sb-latest-articles__tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-bottom: 32px;
  }
  
  .sb-latest-articles__tags-tag {
    padding: 4px 12px 6px;
    font-size: 15px;
    line-height: 130%;
    border-radius: 100px;
    border: 1px solid rgba(0, 0, 0, 0.2);
    font-weight: 400;
    cursor: pointer !important;
    transition: all 0.2s ease;
    background-color: transparent;
    color: rgba(17, 19, 20, 0.80);
  }
  
  .sb-latest-articles__tags-tag[data-tab-active="true"] {
    background-color: var(--sb-fg-color, #000);
    color: #fff;
    border-color: var(--sb-fg-color, #000);
  }

  .sb-latest-articles .swiper-slide {
    height: auto;
  }

  .sb-latest-articles__view-all--active,
  .sb-latest-articles__view-all.active .sb-latest-articles__view-all--inactive {
    display: none;
}

  .sb-latest-articles__view-all.active .sb-latest-articles__view-all--active {
    display: block;
  }
  [data-tab-content="slider"] .sb-latest-articles--grid {
    display: none;
  }
  [data-tab-content="grid"] .sb-latest-articles--slider {
    display: none;
  }

  sb-tabbed-articles {
    position: relative;
    display: block;
    overflow: hidden;
  }

  .sb-latest-articles-loading {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 100;
    background-color: rgba(255, 255, 255, 0.5);
    backdrop-filter: blur(4px);
    transition: opacity 0.2s ease;
    opacity: 0;
    pointer-events: none;
  }

  sb-tabbed-articles[data-loading="true"] .sb-latest-articles-loading {
    opacity: 1;
    pointer-events: unset;
  }

  .sb-latest-articles-loading .loading-spinner {
    width: 40px;
    height: 40px;
    border: 4px solid #ccc;
    border-top-color: rgb(204, 204, 204);
    border-top-color: #000;
    border-radius: 50%;
    animation: spin .6s linear infinite;
  }

  @keyframes spin {
    to {
      transform: rotate(360deg);
    }
  }

  .sb-latest-articles .sb-pagination-wrapper {
    padding: 48px var(--sb-padding-inline) 0;
    max-width: var(--page-width, 1420px);
  }

  .sb-pagination-wrapper .swiper-pagination.swiper-pagination-lock,
  .swiper-pagination-lock + .sb-pagination-count {
    display: none;
  }

  /* Desktop Styles */
  @media screen and (min-width: 768px) {
    .sb-latest-articles__title {
      font-size: 36px;
    }
    .sb-latest-articles__container .sb-latest-articles__tags {
      gap: 12px;
      margin-bottom: 32px;
    }
    
    .sb-latest-articles__tags-tag {
      font-size: 16px;
    }
  }
  
  @media screen and (min-width: 1024px) {
    .sb-latest-articles__container .sb-latest-articles__header {
      margin-bottom: 32px;
    }
  }

  @media (min-width: 1440px) {
    .sb-latest-articles--slider {
      max-width: calc(100% - ((100% - var(--page-width, 1420px)) / 2));
      margin-left: auto;
    }
    .sb-latest-articles .sb-pagination-wrapper {
      margin-right: calc((100vw - var(--page-width, 1420px)) / 2);
    }
  }

  @media (min-width: 1920px) {
    .sb-latest-articles--slider {
      max-width: calc(100% - ((100% - var(--page-width, 1420px) - 128px) / 2));
    }
  }
{% endstylesheet %}

<script
  src="{{ 'section-sb-tabbed-articles.js' | asset_url }}"
  defer
></script>

{%  assign active_blog = blog %}

{% if active_blog != blank and active_blog.articles.size > 0 %}
  {%- liquid
    assign all_tags = ''

    paginate active_blog.articles by 50
      for article in active_blog.articles
        for tag in article.tags
          unless all_tags contains tag
            assign all_tags = all_tags | append: tag | append: '||'
          endunless
        endfor
      endfor
    endpaginate

    assign tag_array = all_tags | split: '||' | uniq
    assign metafield_tags = active_blog.metafields.custom.blog_tab_details.value | parse_json

    assign filtered_tags = ''

    if metafield_tags != blank
      for metafield_tag in metafield_tags
        assign tag_to_match = metafield_tag.tag_to_match
        if tag_array contains tag_to_match
          assign filtered_tags = filtered_tags | append: tag_to_match | append: '||'
        endif
      endfor
    else
      assign filtered_tags = tag_array | slice: 0, 5 | join: '||'
    endif
    assign final_tags = filtered_tags | split: '||' | uniq
  -%}

  <sb-tabbed-articles data-blog-url="{{ active_blog.url }}" data-section-id="{{ section.id }}" data-loading="false">
    <div class="sb-latest-articles-loading">
      <div class="loading-spinner">&nbsp;</div>
    </div>
    <div class="sb-latest-articles sb-styled-element" style="{%  render 'style-attributes', settings: section.settings %}">
      <div class="sb-latest-articles__container">
        <div class="sb-latest-articles__header sb-page-width">
          {% if section.settings.heading != blank %}
            <h2 class="sb-latest-articles__title">{{ section.settings.heading }}</h2>
          {% endif %}

          <a data-view-all class="sb-latest-articles__view-all" href="{{ base_blog.url }}">
            <span class="sb-latest-articles__view-all--inactive">{{ section.settings.link_text | default: "View All" }}</span>
            <span class="sb-latest-articles__view-all--active">{{ section.settings.active_link_text | default: "View Less" }}</span>
            <svg class="sb-featured-videos__view-all-arrow" viewBox="0 0 22 18" fill="none">
              <path d="M13.5 18L13.5 2M13.5 2L17.5 6.25M13.5 2L9.5 6.25" stroke="currentColor" stroke-width="1.29" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
        </div>

        <div class="sb-latest-articles__tags sb-page-width">
          <a 
            data-tab-selector
            data-tab-active="true"
            data-tab="all"
            class="sb-latest-articles__tags-tag"
            >
            All
          </a>
          {% if final_tags.size > 0 %}
            {% for tag_to_match in final_tags %}
              {% if tag_to_match != blank %}
                {%- assign tag_details = metafield_tags | where: "tag_to_match", tag_to_match -%}
                {% if tag_details.size > 0 %}
                  {% assign tag_name_to_display = tag_details[0].tag_name_to_display %}
                  <a 
                    data-tab-selector
                    data-tab-active="false"
                    data-tab="{{ tag_to_match | handle }}"
                    class="sb-latest-articles__tags-tag"
                  >
                   {% if tag_name_to_display != blank %}{{ tag_name_to_display }}{% else %}{{ tag_to_match }}{% endif %}
                  </a>
                {% else %}
                  <a 
                    data-tab-selector
                    data-tab-active="false"
                    data-tab="{{ tag_to_match | handle }}"
                    class="sb-latest-articles__tags-tag"
                  >
                    {{ tag_to_match }}
                  </a>
                {% endif %}
              {% endif %}
            {% endfor %}
          {% endif %}
        </div>
        {%- paginate active_blog.articles by 12 -%}
          <div data-tab-content="slider" data-tab-active="true">
            <div class="sb-latest-articles sb-latest-articles--slider">
              {% liquid
                assign slides = null | sort
                assign articles_count = 0
                for article in active_blog.articles
                  assign articles_count = articles_count | plus: 1
                  capture slide_content
                    content_for 'block', type: '_blog-card', id: 'static-blog-card', article: article, blog: blog
                  endcapture
                  capture slide_content
                    render 'swiper-slide', children: slide_content
                  endcapture
                  assign slide_content = slide_content | sort
                  assign slides = slides | concat: slide_content

                  if articles_count == section.settings.initial_shown_count
                    break
                  endif
                endfor
                if slides.size > 0
                  content_for 'block', type: '_slider', id: 'static-slider', children: slides, mobile_offset: true
                else
                  render 'sb-no-content', context: "articles"
                endif
              %}
            </div>
            <div class="sb-latest-articles sb-latest-articles--grid sb-page-width">
              <div class="sb-latest-articles__grid grid grid--1-col grid--3-col-desktop">
                {% for article in active_blog.articles %}
                  <div class="grid__item">
                    {% content_for 'block', type: '_blog-card', id: 'static-blog-card', article: article, blog: blog %}
                  </div>
                {% endfor %}
              </div>
              <div class="pagination-content">
                {%- if paginate.pages > 1 -%}
                  {%- render 'pagination', paginate: paginate, show_text: true -%}
                {%- endif -%}
              </div>
            </div>
          </div>
        {% endpaginate %}
      </div>
    </div>
  </sb-tabbed-articles>
{% endif %}

{% schema %}
{
  "name": "SB Latest Articles",
  "settings": [
    {
      "type": "header",
      "content": "General"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading"
    },
    {
      "type": "text",
      "id": "link_text",
      "label": "Link Text"
    },
    {
      "type": "text",
      "id": "active_link_text",
      "label": "Active Link Text"
    },
    {
      "type": "range",
      "id": "initial_shown_count",
      "label": "Articles Shown Initially",
      "max": 6,
      "min": 1,
      "step": 1,
      "unit": "Crd",
      "default": 3
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "paragraph",
      "content": "Main Section Colors"
    },
    {
      "type": "color",
      "id": "fg_color",
      "label": "Color"
    },
    {
      "type": "color_background",
      "id": "bg_color",
      "label": "Background Color"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent Color"
    },
    {
      "type": "paragraph",
      "content": "Card Colors"
    },
    {
      "type": "color",
      "id": "ch_fg_color",
      "label": "Color"
    },
    {
      "type": "color_background",
      "id": "ch_bg_color",
      "label": "Background Color"
    },
    {
      "type": "color",
      "id": "ch_accent_color",
      "label": "Accent Color"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding Top",
      "default": 0,
      "min": 0,
      "max": 100
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding Bottom",
      "default": 0,
      "min": 0,
      "max": 100
    }
  ],
  "presets": [
    {
      "name": "SB Latest Articles",
    }
  ]
}
{% endschema %}
